import React from "react"
import { render } from "@testing-library/react"
import App from "../App"

it("responds correctly to theme change", () => {
  //   const { getByTestId } = render()
})

test("should respond correctly to theme change", () => {
  // const {} = render(<App />)
})
